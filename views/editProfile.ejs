<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Knowlytics - Edit Profil</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
      }
    </style>
  </head>
  <body class="bg-gray-100 min-h-screen flex flex-col">
    <%- include('partials/navbar') %>

    <div class="flex-grow flex items-center justify-center py-12">
      <div class="w-full max-w-lg p-8 bg-white rounded-xl shadow-md">
        <h1 class="text-3xl font-bold text-gray-900 mb-2 text-center">
          Perbarui Profilmu
        </h1>
        <p class="text-gray-600 mb-6 text-center">
          Sesuaikan informasi agar belajar lebih nyaman.
        </p>
        <% if (error) { %>
        <div class="bg-red-100 text-red-700 p-3 rounded-lg mb-6 text-center">
          <%= error %>
        </div>
        <% } %>
        <form action="/edit-profile" method="POST" class="space-y-6">
          <div>
            <label for="username" class="block text-gray-700 font-medium"
              >Nama Pengguna</label
            >
            <input
              type="text"
              name="username"
              id="username"
              value="<%= user.username || '' %>"
              class="w-full p-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
              placeholder="Nama pengguna baru"
            />
            <% if (errors && errors.username) { %>
            <p class="text-red-600 text-sm mt-1"><%= errors.username %></p>
            <% } %>
          </div>
          <div>
            <label for="email" class="block text-gray-700 font-medium"
              >Email</label
            >
            <input
              type="email"
              name="email"
              id="email"
              value="<%= user.email %>"
              class="w-full p-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
              placeholder="Email aktif"
            />
            <% if (errors && errors.email) { %>
            <p class="text-red-600 text-sm mt-1"><%= errors.email %></p>
            <% } %>
          </div>
          <div>
            <label for="fullName" class="block text-gray-700 font-medium"
              >Nama Lengkap</label
            >
            <input
              type="text"
              name="fullName"
              id="fullName"
              value="<%= profile ? profile.fullName : '' %>"
              class="w-full p-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
              placeholder="Nama lengkapmu"
            />
            <% if (errors && errors.fullName) { %>
            <p class="text-red-600 text-sm mt-1"><%= errors.fullName %></p>
            <% } %>
          </div>
          <div>
            <label for="phone" class="block text-gray-700 font-medium"
              >Nomor Telepon</label
            >
            <input
              type="text"
              name="phone"
              id="phone"
              value="<%= profile ? profile.phone : '' %>"
              class="w-full p-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
              placeholder="Nomor telepon aktif"
            />
            <% if (errors && errors.phone) { %>
            <p class="text-red-600 text-sm mt-1"><%= errors.phone %></p>
            <% } %>
          </div>
          <button
            type="submit"
            class="w-full bg-blue-600 text-white py-3 rounded-lg font-medium hover:bg-blue-700 transition"
          >
            Simpan Perubahan
          </button>
        </form>
        <div class="mt-6 text-center">
          <a
            href="/delete-user"
            class="text-red-500 font-medium hover:underline"
            onclick="return confirm('Yakin ingin menghapus akun? Data akan hilang selamanya.')"
            >Hapus Akun</a
          >
        </div>
      </div>
    </div>
  </body>
</html>
